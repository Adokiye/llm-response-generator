{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "apps/**/.next/**",
        "apps/**/dist/**"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
